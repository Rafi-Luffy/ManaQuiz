{
  "name": "manaquiz",
  "version": "1.0.0",
  "private": true,
  "description": "Smart quiz generator with advanced file processing capabilities",
  "scripts": {
    "build": "cd apps/web && npm run build",
    "dev": "cd apps/web && npm run dev",
    "lint": "cd apps/web && npm run lint",
    "type-check": "cd apps/web && npm run type-check",
    "clean": "rm -rf node_modules apps/web/node_modules apps/web/dist"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "packageManager": "npm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
